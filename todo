//Complete
-allow edit in place via double clicking an item in the list checked for 0 length strings
-on change mark doc as dirty
	delete
	in place edit
	add item
-remove resizable for doc
-added pop confirmation on delete item
-if user clicks add button, new item text remains, allowing user to delete manually. if item is 
added via on enter, text is removed
-added new datastore, as an array of dictionaries
-check for duplicate item before adding

//Todo
-save and load docs with new datastore
-add status label to give user feedback